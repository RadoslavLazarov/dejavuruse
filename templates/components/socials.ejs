<% navCategories.headerSocials.forEach(function(social) { %>
<a href="<%= social.link %>" target="blank" data-color="<%= social.color %>">
    <i class="<%= social.icon %>"></i>
    <div></div>
</a>
<% }); %>